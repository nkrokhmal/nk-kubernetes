apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: default
spec:
  type: ExternalName
  externalName: grafana.monitoring.svc.cluster.local
  ports:
  - port: 3000

---

apiVersion: v1
kind: Service
metadata:
  name: kibana
  namespace: default
spec:
  type: ExternalName
  externalName: kibana.monitoring.svc.cluster.local
  ports:
  - port: 5601
